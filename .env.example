# Dify 工作流定时调度脚本配置文件示例
# 复制此文件为 .env 并填写你的配置

# Dify API 基础地址（必填）
# 注意：不要包含 /workflows/run 等端点路径
DIFY_BASE_URL=http://xxx.xxx.xxx/v1

# Dify API 端点（可选，默认为 /workflows/run）
# 工作流应用使用: /workflows/run
# 聊天应用使用: /chat-messages
DIFY_API_ENDPOINT=/workflows/run

# Dify 工作流 API Token（必填）
DIFY_TOKEN=app-XXXXXX

# 工作流输入参数（JSON 格式，必填）
# 根据你的工作流定义的变量填写，例如：
# DIFY_INPUTS={"emails":"hayates345@gmail.com"}
# DIFY_INPUTS={"variable1":"value1","variable2":"value2"}
DIFY_INPUTS={}

# 用户标识（可选，默认为 root）
DIFY_USER=root

# 响应模式（可选，默认为 streaming）
# 可选值: streaming（流式，推荐）, blocking（阻塞式）
DIFY_RESPONSE_MODE=streaming

